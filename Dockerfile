# Root Dockerfile for MidiShuffle project
# This is just a reference file - the actual build happens in docker/Dockerfile

# To build the project, run:
# docker build -f docker/Dockerfile -t midishuffle .

# To run the container:
# docker run -p 80:80 midishuffle